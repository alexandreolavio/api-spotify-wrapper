/*
  'SpotifyWrapper - Search'
  - smoke tests'
    - artists
    - albums
    - tracks
    - playlists

    - artists
      - 'should call fetch function'
      - 'should call fetch with the correct URL'

    - albums
      - 'should call fetch function'
      - 'should call fetch with the correct URL'

    - tracks
      - 'should call fetch function'
      - 'should call fetch with the correct URL'

    - playlists
      - 'should call fetch function'
      - 'should call fetch with the correct URL'
*/
